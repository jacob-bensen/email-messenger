-- Internal team notes were removed from the product entirely. Drop the table;
-- the feature, its UI, services, and admin metrics are gone (see V25 for the
-- original creation).
DROP TABLE IF EXISTS thread_notes;
