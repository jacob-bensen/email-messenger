# BACKLOG

Up to 10 items, each advancing a PLAN.md milestone. Pick from the top.

- **EPIC-15 M3 — per-step upgrade nudges in the progress card.**
  After step 2/3/4 completion, swap sub-copy for the corresponding
  Personal/Team upgrade CTA via existing `UpgradeModal`. Advances:
  Per-step upgrade nudges that monetize the steps directly.

- **EPIC-15 M4 — onboarding-funnel card on `/admin/revenue`.**
  Last-30-day signup → mailbox → 10-threads → saved-search →
  invite-sent → paid table anchored on existing timestamps. Advances:
  Operator dashboard card for onboarding-step conversion.
