# BACKLOG

Up to 10 items, each advancing a PLAN.md milestone. Pick from the top.

1. **Upgrade modal at free-tier limit** — when `PlanLimitExceededException`
   is thrown during a controller-driven import, render a plan-comparison
   modal over the thread list with a one-click "Upgrade to Personal" CTA
   into Checkout. (Milestone 4)
